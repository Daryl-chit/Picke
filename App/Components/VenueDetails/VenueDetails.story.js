import React from 'react'
import { storiesOf } from '@storybook/react-native'

import VenueDetails from './VenueDetails'

storiesOf('VenueDetails')
  .add('Default', () => (
    <VenueDetails />
  ))
