import React from 'react'
import { storiesOf } from '@storybook/react-native'

import ProgressLine from './ProgressLine'

storiesOf('ProgressLine')
  .add('Default', () => (
    <ProgressLine />
  ))
