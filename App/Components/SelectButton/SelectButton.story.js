import React from 'react'
import { storiesOf } from '@storybook/react-native'

import SelectButton from './SelectButton'

storiesOf('SelectButton')
  .add('Default', () => (
    <SelectButton />
  ))
