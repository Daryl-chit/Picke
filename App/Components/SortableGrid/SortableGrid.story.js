import React from 'react'
import { storiesOf } from '@storybook/react-native'

import SortableGrid from './SortableGrid'

storiesOf('SortableGrid')
  .add('Default', () => (
    <SortableGrid />
  ))
