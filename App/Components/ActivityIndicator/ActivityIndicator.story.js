import React from 'react'
import { storiesOf } from '@storybook/react-native'

import ActivityIndicator from './ActivityIndicator'

storiesOf('ActivityIndicator')
  .add('Default', () => (
    <ActivityIndicator />
  ))
