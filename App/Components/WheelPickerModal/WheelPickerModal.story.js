import React from 'react'
import { storiesOf } from '@storybook/react-native'

import WheelPickerModal from './WheelPickerModal'

storiesOf('WheelPickerModal')
  .add('Default', () => (
    <WheelPickerModal />
  ))
